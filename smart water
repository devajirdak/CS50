/* C program that prompts the user for the length of
his or her shower in minutes (as a positive integer) 
and then prints the equivalent number of bottles of 
water (as an integer) per the sample output below, 
wherein underlined text represents some user’s input.*/

#include <cs50.h>
#include <stdio.h>

int main(void)
{
    printf("minutes: ");
    int n = GetInt();
    int y;
    y = n * 12;
    printf("bottles:" "%d\n", y);
}
